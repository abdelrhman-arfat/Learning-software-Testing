export const sum = (n1: number, n2: number) => n1 + n2;
export const sub = (n1: number, n2: number) => (n1 > n2 ? n1 - n2 : n2 - n1);
export const mul = (n1: number, n2: number) => n1 * n2;
export const div = (n1: number, n2: number) => n1 / n2;
